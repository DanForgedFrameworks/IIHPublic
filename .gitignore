# IIH UK Resource Library — .gitignore
# Local master stays in OneDrive. GitHub receives published output only.

# Internal document control — stays local, never push
_Admin/

# Working session screenshots — not publishable assets
Screens/

# Video files — too large for GitHub (host on YouTube/embed externally)
Videos/

# Superseded versions — local archive only
00 Archive/

# Staging area for new/unprocessed files — never publish
To be processed/

# Newsletter working area — internal briefs and previous editions, not yet
# catalogued. Was unprotected until 20260804; a broad `git add` would have
# published the team brief. Remove this line only when a newsletter asset is
# formally catalogued and intended for release.
04 Newsletter/

# Claude AI project files — internal AI session management, stays local
.claude/
CLAUDE.md

# Secrets — this repo is PUBLIC. A `.env` holding a live Clockify API key sat
# untracked and unignored here until 20260911; a broad `git add` would have
# published it. The key now lives outside every repo, at ~/.secrets/. Nothing
# of this kind belongs in this working tree — these lines are a backstop, not
# a licence to keep credentials here.
.env
*.env
.env.*
secrets/
.secrets/

# Windows system files
Thumbs.db
Thumbs.db:encryptable
desktop.ini
*.lnk
$RECYCLE.BIN/

# Office temporary files
~$*
*.tmp

# Mac system files (in case contributors use Macs)
.DS_Store
.AppleDouble
.LSOverride
__MACOSX/

# OneDrive conflict files
* (1).*
* (2).*

# Temp base64 working files
logo_base64.txt
